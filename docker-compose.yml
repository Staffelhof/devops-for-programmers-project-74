services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.production
    image: samkapandbl2/devops-for-programmers-project-74:latest
    container_name: devops-for-progs
    ports:
      - "8080:8080"
    command: make test